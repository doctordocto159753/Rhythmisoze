# Production GAME v1.0.3 large ONNX backend.
services:
  transcription:
    environment:
      GAME_BACKEND: onnx
      GAME_MODEL_TIER: large
      GAME_MODEL_VERSION: 1.0.3
      TRANSCRIPTION_MODEL_DIR: /models/game-large-onnx/GAME-1.0.3-large-onnx
      GAME_ONNX_THREADS: "4"
