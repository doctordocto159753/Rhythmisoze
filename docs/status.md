# Implementation status

Story-by-story state against `02_ENGINEERING_WORK_PACKAGE.md` and
`03_DESIGN_WORK_PACKAGE.md`.

**Legend**
- **Done** — acceptance criteria met and covered by a test or an inspectable artifact.
- **Built, unverified** — implemented, but an acceptance criterion needs a device, a corpus or a human that was not available.
- **Not done** — deliberately not implemented; the reason is stated.

---

## WP-00 — Bootstrap

| Story | State | Where / why |
|---|---|---|
| US-0001 freeze scope | Done | `docs/product-decisions.md`; defaults marked where the questionnaire was silent |
| US-0002 repo and quality baseline | Done | One `npm ci` bootstrap; strict TS, ESLint, Vitest, CI on every PR |
| US-0003 ADR system | Done | `docs/adr/` with four records and a template; ADR-001 reserved as required |
| US-0004 benchmark corpus | Partial | First owner human take added as a privacy-preserving derived regression fixture; a representative multi-speaker corpus is still required |
| US-0005 architecture gate | **Not done** | Blocked on US-0004. ADR-001 records the gap explicitly and is marked not-for-release |

## WP-01 — Shell, i18n, foundations

| Story | State | Where |
|---|---|---|
| US-0101 bootstrap Next.js | Done | Next 16.3.1 pinned; production build passes; client-only audio/WebGL never runs on the server |
| US-0102 bilingual routing | Done | `[locale]` segment, `proxy.ts` negotiation, cookie memory; a missing key is a type error |
| US-0103 bidirectional primitives | Done | Logical properties throughout; `<Bdi>`; time axis pinned LTR; specimens in the catalog |
| US-0104 state machine | Done | `features/state/machine.ts`, 26 tests including the full reachability graph |
| US-0105 capability detection | Done | `capabilities.ts` + `useCapabilities` via `useSyncExternalStore`; missing pieces named to the user |

## WP-02 — Tempo, metronome, recording

| Story | State | Where |
|---|---|---|
| US-0201 tap tempo | Done | Pure and tested: outlier rejection, phrase reset, rolling window |
| US-0202 tempo slider | Done | Keyboard and touch; stays in sync with taps |
| US-0203 low-drift metronome | Built, unverified | Web Audio lookahead scheduler; `measureDrift` tested; not measured on device |
| US-0204 count-in | Done | One measure at the selected meter; recording starts on the audio clock |
| US-0205 capture constraints | Built, unverified | EC/NS/AGC off with documented reasoning; granted settings recorded; needs device testing |
| US-0206 60 s lifecycle | Built, unverified | Hard cap in the recorder; empty capture rejected; needs device testing |
| US-0207 live waveform | Done | Throttled to 30 Hz; text equivalent for AT |
| US-0208 input validation | Built, unverified | Calibrated multi-signal gate; thresholds reasoned, not measured on humans |

## WP-03 — Melody transcription

| Story | State | Where |
|---|---|---|
| US-0301 normalize audio | Done | `MonoAudio`, deterministic downmix, diagnostics, resampling |
| US-0302 model load and cache | Built, unverified | Self-hosted 0.9 MB model, versioned, warmed on record hover; load time not measured |
| US-0303 off the main thread | Done | Worker with typed protocol, real cancellation, an escaped-error trap and a watchdog; verified end to end in Chromium by `tests/e2e/capture.spec.ts` |
| US-0304 result contract | Done | Voice uses the isolated Human Melody Extraction Engine; Instrument Mode keeps polyphonic Basic Pitch; rhythm and MIDI import remain explicit |
| US-0305 progress and recovery | Done | Real stages, typed errors, retry without re-recording |
| US-0306 server adapter | **Not done** | Correctly conditional on ADR-001 selecting a server path. It did not. |

## WP-04 — Retouch port

| Story | State | Where |
|---|---|---|
| US-0401 golden reference | Done | `reference/humtool.py` vendored; 10 fixture cases generated by running it; CI regenerates and diffs |
| US-0402 octave filtering | Done | Golden parity without a guide; melody mode uses trusted YIN centre and a post-Clean quality guard |
| US-0403 quantization | Done | Parity at 1/8, 1/16, 1/32, including round-half-to-even |
| US-0404 key detection | Done | Krumhansl-Schmuckler parity including NaN on a flat histogram; confidence floor calibrated against the corpus |
| US-0405 scale snapping | Done | Parity at full strength; partial strength ranks by duration, monotonic and tested |
| US-0406 short-note merge | Done | Extension beyond the port; floor capped at half a grid step so real subdivisions survive |
| US-0407 Raw→Clean macro | Done | One control → seven parameters; monotonicity asserted across all 101 values |
| US-0408 diagnostics | Done | In the review details panel, optional and out of the way |

## WP-05 — Rhythm

| Story | State | Where |
|---|---|---|
| US-0501 taxonomy | Done | kick/snare/hat + `unknown`, with a documented handling rule |
| US-0502 onset detection | Built, unverified | Spectral flux, adaptive threshold, double-trigger guard; tested on synthetic hits only |
| US-0503 classification | Built, unverified | Rule-based, licence-clean, explainable; not evaluated on human beatboxing |
| US-0504 quantize rhythm | Done | Same grid and strength as melody; collision rule explicit and tested |
| US-0505 GM mapping | Done | Export and playback agree, asserted by parsing the written file |

## WP-06 — Synthesis

| Story | State | Where |
|---|---|---|
| US-0601 engine selection | Done | Sample-first with deterministic procedural fallback; UI depends only on neutral registry metadata |
| US-0602 registry and licences | Done | 14 instruments, including six recorded acoustic packs; manifests, hashes and licence ledger are audited |
| US-0603 lazy loading | Done | Initial navigation fetches no packs; explicit selection reports progress and caches the decoded pack |
| US-0604 preview | Done | One at a time, keyboard reachable, per-instrument gesture |
| US-0605 playback | Done | Deterministic; instrument change never mutates note data |
| US-0606 offline render | Done on desktop | Browser E2E exports non-silent stereo 44.1 kHz/16-bit WAV from a selected sample pack; real-device matrix remains open |
| US-0607 volume/reverb | Done | Two controls, sensible defaults, generated impulse |

## WP-07 — Review

| Story | State | Where |
|---|---|---|
| US-0701 piano roll | Done | SVG, no editing affordances, semantic summary for AT |
| US-0702 raw vs cleaned | Done | Raw transcription drawn behind the result; original always recoverable |
| US-0703 instrument in review | Done | Gallery inline; loading never blocks unrelated controls |
| US-0704 retry path | Done | Re-record preserves BPM, mode and instrument; transitions tested |
| US-0705 microcopy | Done | Both locales; no DAW vocabulary; every error offers a next action |

## WP-08 — Local workspace

| Story | State | Where |
|---|---|---|
| US-0801 IndexedDB schema | Done | Dexie, versioned, rendered/MIDI/original-source blobs in a separate local-only table |
| US-0802 autosave | Done | Debounced, only from `review` onward, never persists a transient state |
| US-0803 list and reopen | Done | Contour drawn from the sketch's own notes; empty state teaches the first action |
| US-0804 rename and delete | Done | Confirmation step; blobs cleaned; published copy untouched unless asked |
| US-0805 quota and eviction | Done | Notes survive when audio cannot be stored; persistence requested; no silent deletion |

## WP-09 — Export

| Story | State | Where |
|---|---|---|
| US-0901 MIDI | Done | Tempo, meter, program, GM drum channel — all asserted by re-parsing |
| US-0902 WAV | Done | 16-bit PCM documented; no watermark; header round-trip tested |
| US-0903 export UX | Done | Complete ZIP contains rendered WAV, MIDI, manifest and untouched source; individual downloads remain available |
| US-0904 MP3 spike | **Not done** | P2, explicitly allowed to be deferred |

## WP-10 — Publishing

| Story | State | Where |
|---|---|---|
| US-1001 identity model | Done | ADR-004, per Q-C1 |
| US-1002 storage and database | Done | Blob + Postgres, idempotent migrations, documented env |
| US-1003 direct upload | Done | Server-minted id, signed ticket, scoped token, path-verified |
| US-1004 create record | Done | Duplicate-safe; ticket re-verified before any write |
| US-1005 share page | Done | Server-rendered, branded, CTA present, deleted state handled |
| US-1006 social card | Done | `@vercel/og`, deterministic waveform, long titles in both scripts |
| US-1007 delete | Done | Constant-time token compare; identical 404 for every failure mode |
| US-1008 abuse controls | Done, with a stated limit | Rate limits are per-instance; runbook says what to do about it |

## WP-11 — Analytics and privacy

| Story | State | Where |
|---|---|---|
| US-1101 event taxonomy | Done | 20 events, narrow property type, documented |
| US-1102 funnel | Done | Landing→render computable; failures categorised; never blocks creation |
| US-1103 performance telemetry | Done | Model load, transcription, render, backend, coarse device class |
| US-1104 privacy disclosure | Done | Local claim on every screen; a separate, different claim at publish |

## WP-12 — Hardening

| Story | State | Where |
|---|---|---|
| US-1201 keyboard and SR | Built, unverified | Full keyboard path, live regions, canvas equivalents; no manual SR test run |
| US-1202 reduced motion and 3D fallback | Done | Three tiers, designed fallback, identical interactions |
| US-1203 browser matrix | Built, unverified | Playwright across five projects, plus a capture project driving a fake microphone; no real-device microphone run |
| US-1204 budgets | Built, desktop instrument gate verified | Selected sample pack passes the automated <5 s Chromium gate; the wider real-device matrix remains open |
| US-1205 security review | Done | Threats and mitigations in ADR-004; 34 tests written as attacks |
| US-1206 failure and recovery | Done | Offline creation intact, retry paths, localized errors |

## WP-13 — CI/CD

| Story | State | Where |
|---|---|---|
| US-1301 preview per change | Done | CI workflow; runbook requires isolated preview resources |
| US-1302 production and migrations | Done | `docs/runbooks/deployment.md`, idempotent migrate script |
| US-1303 monitoring | **Not done** | Q-G3 unanswered. Recommended before public release. |
| US-1304 rollback | Done | `docs/runbooks/rollback.md`; additive-migration policy makes it safe |
| US-1305 release acceptance | **Not done** | Requires the device matrix and the architecture gate |

## Design package

| Story | State |
|---|---|
| D-0001 three territories | Done — DD-001 |
| D-0002 visual thesis | Done — five invariants, in the token file |
| D-0003 sensory mappings | Done — DD-002 mapping table |
| D-0101…D-0105 type, colour, space, shape, motion | Done — `tokens.css` |
| D-0201…D-0203 IA, onboarding, mode | Done |
| D-0301…D-0304 tempo, count-in, recording, guidance | Done |
| D-0401…D-0403 processing, raw-to-clean, piano roll | Done |
| D-0501…D-0502 gallery, sonic identity | Done — spectral figures from the voice recipes |
| D-0601…D-0604 workspace, export, publish, share | Done |
| D-0701…D-0705 3D role map, object, pulse, materials, ladder | Done — DD-002 |
| D-0801 catalog | Done — DD-003 records the Storybook deviation |
| D-0802 visual regression | Partial — stable anchors and E2E coverage; no screenshot baseline |
| D-0803 holistic audit | Not done — needs a human |
| D-0901…D-0903 accessibility, bilingual, mobile audits | Built, unverified — needs a human on real devices |
| D-1001 handoff spec | Done — the catalog plus DD-001/002 |

---

## Engineering baseline

Restored on 2026-08-21 (branch `fix/ci-and-musician-foundation`). Verified by
reproducing every CI gate in Docker on Linux with the CI's own Node 20 and
Python 3.12, not on the development machine.

| Gate | State | Evidence |
|---|---|---|
| `npm ci` | **green** | Was red: the lockfile was generated on Windows and omitted `@emnapi/core` and `@emnapi/runtime`, which Linux resolves via `@img/sharp-wasm32`. Repaired on Linux; exactly 2 entries added, 0 removed, 0 version changes. |
| typecheck | green | Linux, Node 20.20.2 |
| lint | green | Linux, Node 20.20.2 |
| Vitest + coverage | green | 513 tests, 23 files |
| production build | green | Linux, Node 20.20.2 |
| humtool golden regeneration | green | Clean diff under Python 3.12. **Sensitive to the Python minor version** — 3.11 produces a last-bit float difference in `gridError`, because CPython 3.12 changed `sum()` to compensated summation. CI pins 3.12; the generator now documents this. |
| retouch parity suite | green | 181 assertions, unchanged |
| Playwright matrix | green | 131 passed, 22 skipped, 0 failed on the GitHub runner (run `32438318388`), and in the CI container at `--cpus=2` |

### E2E skips are paired with assertions

22 skips, all capability-gated, and none of them silent. Playwright's Linux
WebKit does not implement `MediaRecorder` — probed directly it reports
`undefined`, while Chromium and Firefox report a function — so the app correctly
renders its unsupported-browser panel and the creation screen never exists.

`tests/e2e/unsupported-browser.spec.ts` asserts the other side of that same
condition: that the fallback appears, names what is missing, stays navigable and
is localized. Every browser in the matrix now makes a positive assertion in one
direction or the other.

### Three real test defects fixed, and one removed race

- **A crashed browser session reported as a failed assertion.** The too-short-take
  test clicked Stop in the same tick recording started. Stopping a MediaRecorder
  that has emitted nothing killed the Chromium session (`Protocol error: session
  closed`) - a state no user can reach.
- **A locator matching Next's route announcer.** `getByRole('alert')` resolved to
  both the error panel and `__next-route-announcer__`, tripping strict mode
  intermittently. The product was correct every time it "failed".
- **A test that could not be made deterministic.** After the two fixes above, the
  same test still failed on the GitHub runner, this time because the take was
  *accepted*: a 350 ms sleep plus click latency exceeded the 750 ms floor there,
  so no refusal ever appeared. The length of a real recording is decided by
  scheduler latency across the runner, the browser and the page against a 750 ms
  window, and no amount of selector work changes that.

  It was not deleted or skipped. `ingestAudioBlob()` decodes and calls
  `validateAudio()` for a recording and an upload alike, so the refusal is now
  driven by a 0.4 s WAV whose duration is fixed by its header. **Coverage went
  up**: the refusal is asserted deterministically, a new 1.2 s case proves the
  floor is a boundary rather than "short uploads never work", and the recording
  path keeps a test asserting what is genuinely deterministic about it - that
  stopping early always reaches a terminal state and leaves the page usable,
  which is exactly what regressed twice.

### Concurrency is declared

`playwright.config.ts` pins `workers: 2` under CI. Left to the default,
Playwright sizes the pool from the host core count, and at 8 workers the
audio-capture browser sessions die. Every test still runs on every browser.

## Gates

| Gate | State |
|---|---|
| G0 product decisions | **Pass** — resolved or explicitly defaulted |
| G1 architecture benchmark | **Fail** — not run |
| G2 audio vertical slice | **Pass** — record → transcribe → retouch → instrument → playback, asserted end to end in a real browser |
| G3 local MVP | **Pass** — full local creation, workspace, WAV and MIDI |
| G4 publish | **Pass** — publish, share, delete, OG |
| G5 design hardening | **Partial** — system, 3D, motion, RTL done; human audits not run |
| G6 production hardening | **Fail** — no device verification, no monitoring |
| G7 release | **Fail** — blocked on G1 and G6 |

## Next phase

[`architecture/musician-foundation.md`](architecture/musician-foundation.md)
freezes the five-version pipeline (Unprocessed / Judge / Teacher / Musician
Refined / Musician Developed) and the model foundation.
[`../third_party/MANIFEST.md`](../third_party/MANIFEST.md) pins the upstream
revisions and licences. No AI model code or weights are committed.

The product is complete as a working application and is **not** ready for a
production release. The two things standing between those states are the
architecture quality gate and real-device verification, both of which need
more inputs (a broader labelled audio corpus and physical devices) than the
current human regression set provides.
