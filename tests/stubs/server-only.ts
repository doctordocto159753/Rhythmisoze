/**
 * Test stub for Next.js's `server-only` guard.
 *
 * The real package exports nothing; it exists so a bundler errors when a
 * server module is imported into a client bundle. That check is a build-time
 * concern and has no Node runtime, so tests alias it here in order to exercise
 * `src/server/*` directly.
 */
export {};
